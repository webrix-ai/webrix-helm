# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common IDEs
.idea/
.vscode/
*.swp
# Local debug files
debug_output*.yaml
webrix-demo-minimal.yml
webrix-demo_value.yaml
